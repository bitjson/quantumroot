# Quantumroot Test Suite

This test suite verifies each component of the Quantumroot vault contract against itself, against [RFC 8554](https://datatracker.ietf.org/doc/html/rfc8554) test vectors, and against a model Typescript implementation.

To run the tests:

```sh
bun install
bun test
```
